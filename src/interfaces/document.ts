export interface DocumentType {
    
}

export interface Document {
    title: string
    template: string
    doc_type: number
    with_sign_seal: string
    public: string
    sum: number
    status: string
    isGroup: boolean
    parent_id: string
}